#include <iostream>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    int count;
    cin >> count;
    for(int i = 1; i<=count; i++)
        cout << i << '\n';
    
    return 0;
}